---
import { localizeHref, getLangFromParam, pickText } from "../../lib/i18n-utils";

import type { Article } from "../../types";
import { formatDate } from "../../lib/utils";

interface Props {
  lang?: string;
  articles: Article[];
}

const { lang = "en", articles } = Astro.props;
const resolvedLang = getLangFromParam(lang);
const pt = (v: { en: string; el: string } | string) => pickText(v, resolvedLang);
const l = (href: string) => localizeHref(href, lang);
---

<section id="articles" class="py-20" aria-labelledby="articles-heading">
  <div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8">
    <div class="flex items-end justify-between mb-12">
      <div>
        <h2 id="articles-heading" class="text-3xl sm:text-4xl font-bold text-text" data-i18n="articles.title">
          From the blog
        </h2>
        <p class="mt-3 text-text-muted text-lg" data-i18n="articles.subtitle">
          Tips, guides, and stories from the sea
        </p>
      </div>
      <a
        href={l("/blog")}
        class="hidden sm:inline-flex items-center text-sm font-semibold text-primary hover:text-primary-dark transition-colors"
        data-i18n="articles.view_all"
      >
        View all articles
        <svg class="ml-1 h-4 w-4" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
          <path stroke-linecap="round" stroke-linejoin="round" d="M9 5l7 7-7 7"/>
        </svg>
      </a>
    </div>

    <div class="grid grid-cols-1 md:grid-cols-3 gap-6">
      {articles.map((article) => (
        <a href={l(`/blog/${article.slug}`)} class="group block">
          <article class="bg-white rounded-xl overflow-hidden border border-border hover:shadow-lg transition-shadow">
            <div class="aspect-[16/9] overflow-hidden">
              <img
                src={article.image}
                alt={pt(article.title)}
                class="h-full w-full object-cover transition-transform duration-500 group-hover:scale-105"
                loading="lazy"
                decoding="async"
              />
            </div>
            <div class="p-5">
              <time class="text-xs text-text-muted font-medium" datetime={article.createdAt?.toISOString?.() ?? ""}>
                {article.createdAt ? formatDate(article.createdAt) : ""}
              </time>
              <h3 class="mt-2 text-lg font-bold text-text group-hover:text-primary transition-colors line-clamp-2">
                {pt(article.title)}
              </h3>
              <p class="mt-2 text-sm text-text-muted line-clamp-2">
                {pt(article.summary)}
              </p>
            </div>
          </article>
        </a>
      ))}
    </div>

    <div class="mt-8 text-center sm:hidden">
      <a
        href={l("/blog")}
        class="inline-flex items-center text-sm font-semibold text-primary hover:text-primary-dark transition-colors"
        data-i18n="articles.view_all"
      >
        View all articles
        <svg class="ml-1 h-4 w-4" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
          <path stroke-linecap="round" stroke-linejoin="round" d="M9 5l7 7-7 7"/>
        </svg>
      </a>
    </div>
  </div>
</section>
